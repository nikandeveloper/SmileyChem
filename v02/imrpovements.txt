A bidirectional encoder implemented to further reduce the model from forgetting tokens
and counter the down sides of the simple MLP RNN or as i like to call it DeepRNN

an improved tokeniser which is coded with AI assistance is also used to account for tokens like Cl and se in SMILES format
However the architecture and the core idea behind it was mine

